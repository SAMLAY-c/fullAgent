# Project Context

## Commands
- Build: `npm run build` (根据你的项目修改)
- Test: `npm test`
- Lint: `npm run lint`

## Code Style
- TypeScript / React (根据你的项目修改)
- Functional programming patterns preferred.

## Auto-Doc Protocol (MANDATORY)
**本项目已启用 Auto-Doc 自动文档维护模式。**
在执行任何代码修改任务时，你必须首先读取并理解 `.claude/docs/AUTO_DOC_SKILL.md` 中的规则。

**关键要求：**
1. 不要等到我提醒你才写文档。**边写代码边更新文档**。
2. 严格遵守“文件头 -> INDEX.md -> ARCHITECTURE.md”的更新级联顺序。
3. 在提交代码前，请自查：是否更新了对应的 `INDEX.md`？
