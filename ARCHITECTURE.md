# Bot Agent 架构总览

Bot Agent 是一个基于 Node.js + Express 的 AI 聊天机器人管理平台，采用前后端分离架构。

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │ 聊天界面    │  │ 管理界面    │  │ 登录界面    │            │
│  │ chat.js    │  │ admin.js   │  │ login.js   │            │
│  └────────────┘  └────────────┘  └────────────┘            │
└─────────────────────────────────────────────────────────────┘
                           ↓ HTTPS
┌─────────────────────────────────────────────────────────────┐
│                        服务端层                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Express.js HTTP 服务器                   │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │  │
│  │  │ 认证中间件│  │ 路由层   │  │ 服务层           │   │  │
│  │  │ auth.ts  │  │ routes/  │  │ services/        │   │  │
│  │  └──────────┘  └──────────┘  └──────────────────┘   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                │
│  JSON 文件存储 + 数据归档服务                                │
└─────────────────────────────────────────────────────────────┘
```

## 核心模块

### 前端架构
- **公共库**: 详见 [frontend/public/assets/INDEX.md](frontend/public/assets/INDEX.md)
- **页面组件**: 聊天、管理、登录三个主要界面
- **状态管理**: 基于原生 JavaScript 的本地状态管理
- **通信方式**: Fetch API + JWT 认证

### 后端架构
- **路由层** (`backend/src/routes/`): 定义 API 端点和请求处理
- **服务层** (`backend/src/services/`): 业务逻辑实现
- **中间件** (`backend/src/middleware/`): 认证和请求拦截
- **工具层** (`backend/src/utils/`): 通用工具函数

### 数据流
1. 用户操作 → 前端事件处理
2. API 调用 → HTTP 请求 (带 JWT token)
3. 后端路由 → 中间件验证 → 服务层处理
4. 数据持久化 → JSON 文件存储
5. 响应返回 → 前端更新 UI

## 关键技术栈

- **前端**: 原生 JavaScript + HTML5 + CSS3
- **后端**: Node.js + Express + TypeScript
- **认证**: JWT (JSON Web Token)
- **存储**: JSON 文件系统
- **Markdown**: marked.js (用于 AI 消息渲染)

## 最近变更

### 2026-02-22: 前端代码重构
- 创建公共工具库 `frontend/public/assets/lib/utils.js`
- 消除多个文件中的重复函数（escapeHtml、formatTime 等）
- 删除根目录的旧 HTML 文件（统一使用 frontend/public/）
- 删除重复的 MCP 配置文件
